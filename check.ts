#!/usr/bin/env npx tsx
/**
 * typegraph-mcp Health Check — Re-export from src/health/checker.ts
 *
 * This file exists for backward compatibility.
 * The implementation lives in src/health/checker.ts.
 */

export { main, type CheckResult } from "./src/health/checker.js";
